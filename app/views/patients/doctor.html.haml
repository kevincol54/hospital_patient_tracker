%h1 Add Doctor
=form_for [@hospital, @patient, @doctor], url: add_doctor_hospital_patient_path(@hospital, @patient) do |f|
  =f.label :name
  =f.text_field :name
  = f.submit
=link_to "Cancel", hospital_patient_path(@hospital, @patient)
